# typed: strong

class Number; end

class MovieTheaterFee::Purchase
  # sig { params(movie_showing: MovieShowing, number: Number).void }
  # def initialize(movie_title_id:, number: Number)
  # end
end
